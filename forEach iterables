function forEach(list, callback) {
  const entries = list instanceof Map || list instanceof Set
    ? Array.from(list.entries())
    : Object.entries(list);
  
  for(const [key, val] of entries) {
    const res = callback(val, key, list);
    
    if(res === true) break;
  }
}
